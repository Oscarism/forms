# Cloudflare Pages configuration
# Note: This file is used for local development with wrangler
# Cloudflare Pages deployment is handled automatically by the build output

name = "ovaldez-forms"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"

# Output directory for Cloudflare Pages
# The SvelteKit adapter outputs to .svelte-kit/cloudflare
